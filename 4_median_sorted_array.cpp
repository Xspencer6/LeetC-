#include <vector>
#include <algorithm>


double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        std::vector<int> list;

        for (auto num: nums1){
            list.push_back(num);
        }

        for (auto num: nums2){
            list.push_back(num);
        }

        std::sort()
    }
